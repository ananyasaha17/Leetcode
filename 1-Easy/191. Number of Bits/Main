public int hammingWeight(int n) {
        int count = 0;
        String num = Integer.toBinaryString(n);
        int len = num.length();
        for(int i = 0; i < len; i++){
            if(num.charAt(i) == '1') count++;
        }
        return count;
    }
